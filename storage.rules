// Storage kullanılmıyor (free plan).
// Bu dosya sadece referans için bırakıldı.
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
